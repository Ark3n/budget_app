/// Shared constants for the default local account (single-account MVP).
abstract final class BudgetDefaults {
  static const String defaultAccountId = 'default_salary_account';
  static const double initialAccountBalance = 0;
}
